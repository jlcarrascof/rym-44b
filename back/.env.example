# Environment Variables Template for Express Backend & PostgreSQL (Render / Railway / Docker)

PORT=3001
HOST=localhost

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres123
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rickandmorty

# Environment
NODE_ENV=production
